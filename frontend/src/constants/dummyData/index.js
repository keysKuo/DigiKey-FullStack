export const categories = [...Array(10).keys()].map((num) => `category ${num + 1}`);
export const genres = [...Array(10).keys()].map((num) => `genre ${num + 1}`);
export const sortOptions = [
    'Mặc định',
    'Bán chạy nhất',
    'Mới cập nhật',
    'Giá thấp đến cao',
    'Giá cao đến thấp',
    'Tên từ A đến Z',
    'Tên từ Z đến A',
];
    
    